interface:
  display_name: "Code Video"
  short_description: "Research a topic and render a promo video drawn entirely in code, in a chosen visual style"
  default_prompt: "Use $code-video with a style (or a DESIGN.md), the topic, and the format: research with sources, get a short plan approved, then build and render the MP4."
